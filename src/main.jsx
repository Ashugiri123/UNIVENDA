import React from "react";
import ReactDOM from "react-dom/client";
import App from "./CustomerAppShell";
import "./customer-app-shell.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
